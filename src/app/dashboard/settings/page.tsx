import {
  Section,
  SectionTitle,
  SectionHeader,
} from "@/components/content/section";

export default function Page() {
  return (
    <Section>
      <SectionHeader>
        <SectionTitle>Settings</SectionTitle>
      </SectionHeader>
    </Section>
  );
}
